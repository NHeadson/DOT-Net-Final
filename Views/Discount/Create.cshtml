@model Discount

<h2 class="text-center">Create Discount</h2>

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <form asp-action="Create" method="post">
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group mb-3">
                    <label asp-for="Title" class="form-label">Title</label>
                    <input asp-for="Title" class="form-control" />
                </div>
                <div class="form-group mb-3">
                    <label asp-for="ProductId" class="form-label">Product</label>
                    <select asp-for="ProductId" asp-items="@(new SelectList(ViewBag.Products, "ProductId", "ProductName"))" class="form-control"></select>
                </div>
                <div class="form-group mb-3">
                    <label asp-for="DiscountPercent" class="form-label">Discount Percent</label>
                    <input asp-for="DiscountPercent" class="form-control"  type="number" step="0.01" min="0" max="100" />
                </div>
                <div class="form-group mb-3">
                    <label asp-for="StartTime" class="form-label">Start Date</label>
                    <input asp-for="StartTime" type="date" class="form-control" value="@DateTime.Now.ToString("MM-dd-yyyy")" min="@DateTime.Now.ToString("MM-dd-yyyy")" />
                </div>
                <div class="form-group mb-3">
                    <label asp-for="EndTime" class="form-label">End Date</label>
                    <input asp-for="EndTime" type="date" class="form-control" value="@DateTime.Now.ToString("MM-dd-yyyy")" min="@DateTime.Now.ToString("MM-dd-yyyy")" />
                </div>
                <div class="form-group mb-3">
                    <label asp-for="Description" class="form-label">Description</label>
                    <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                </div>
                <button type="submit" class="btn btn-primary w-100">Create</button>
            </form>
        </div>
    </div>
</div>
